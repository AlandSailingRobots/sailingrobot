#include "CANDatalistner.h"
#include "CANService.h"

#include <iostream>

/*CANDatalistner::CANDatalistner(CANService& service, std::vector<uint32_t> IDs){
    for(uint32_t id : IDs) {
      service.registerForReading(*this, id);
    }
}

CANDatalistner::CANDatalistner(CANService& service, uint32_t ID) {
  service.registerForReading(*this, ID);
}
*/
